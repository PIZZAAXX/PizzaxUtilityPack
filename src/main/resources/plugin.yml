name: PizzaxUtilityPack
version: '${version}'
main: pizzaaxx.pizzaxutilitypack.PizzaxUtilityPack

commands:
  pizzaxup:
    description: Reload the plugin's configuration.
    permission: pizzaxutilitypack.admin

permissions:
  pizzaxutilitypack.admin:
    description: "Plugin management permissions."
    default: op